# macOS
.DS_Store

# Godot 4 editor/import cache — regenerated automatically by the editor,
# no need to track it.
game/.godot/

# Raw downloaded asset pack (from itch.io). Not referenced by the Godot
# project itself — the game/ tree already has its own copies of what's used
# (see the per-feature sprites/ folders under game/player, game/objects, etc.).
/Free/
/Pixel Adventure 1.zip
